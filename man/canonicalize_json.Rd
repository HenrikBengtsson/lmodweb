% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonicalize_json.R
\name{canonicalize_json}
\alias{canonicalize_json}
\title{Canonicalize a JSON String}
\usage{
canonicalize_json(json)
}
\arguments{
\item{json}{A JSON-formatted character string.}
}
\value{
A canonical JSON-formatted character string, ending with a
newline.
}
\description{
Rewrites a JSON string into a deterministic, human-readable form: the
keys of every object are sorted, arrays whose elements are themselves
objects or arrays are sorted by their canonical text, the result is
pretty-printed with two-space indentation, and it ends with a newline.
}
\details{
This exists because Lmod's \verb{spider -o jsonSoftwarePage} emits object
keys - and the set-like arrays (the package list, the per-version
\code{parent} list, and same-version build variants) - in hash order, so
re-scanning an unchanged module tree produces a different file every
time.  \code{\link[=spider]{spider()}} passes its output through this function so that the
cache is a pure function of the module tree.

Element order carries no meaning in the spider output, so downstream
parsing is unaffected: \code{\link[=parse_module]{parse_module()}} reduces \code{parent} with
\code{unique(unlist(...))} and keys the default off \code{defaultVersionName},
and \code{\link[=module_avail]{module_avail()}} re-sorts \code{versions} with \code{\link[gtools:mixedorder]{gtools::mixedorder()}}.
A single \code{parent} dependency chain is an array of plain strings and is
left in its given order.
}
\examples{
cat(canonicalize_json('[{"b":2,"a":1},{"b":0,"a":9}]'))

}
